{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://json-schema.myorg.com/cdc_x_1-0-0.schema.json",
  "type": "object",
  "description": "Represents a CDC message for entity X",
  "properties": {
    "curr": {
      "$ref": "entity_x_1-0-0.schema.json"
    },
    "prev": {
      "$ref": "entity_base_1-0-0.schema.json",
      "description": "The datastore may have earlier major versions of this entity, so for safety, just demand that something which is an example of an entity is given."
    }
  },
  "required": ["curr", "prev"]
}
